[project]
name = "trace"
version = "0.1.0"
description = "Minimal distributed issue tracker for AI agent workflows"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=9.0.1",
    "pytest-cov>=7.0.0",
    "ruff>=0.14.5",
    "ty>=0.0.1a26",
]
